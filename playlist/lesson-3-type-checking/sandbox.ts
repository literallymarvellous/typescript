let character = "marvel";
let age = 30;
let isBlackBelt = false;

// can't be changed to new type, remains string always
// character = 20

character = "marvellous";

const circ = (diameter: number) => {
  return diameter * Math.PI;
};

console.log(circ(20));
